server:
  port: 3377
spring:
  application:
    name: nacos-config-client
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848 #nacos服务中心的注册地址
      config:
        server-addr: localhost:8848 # nacos配置中心的地址
        file-extension: yaml  #指定yaml格式的配置
        group: TEST_GROUP